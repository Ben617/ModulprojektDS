You are an information extraction system.

Task:
Predict relations between the provided entities.

You must only use the provided entity IDs.
Do not create new entities.
Do not use entity names that are not listed in the Entities input.

Allowed relation types:

Located_In:
An entity is physically located in a location.

Work_For:
A person works for, is employed by, represents, or is professionally affiliated with an organization.

OrgBased_In:
An organization is headquartered, based, located, or operates in a location.

Live_In:
A person lives or resides in a location.

Kill:
A person killed another person.

Important rules:

* Only use the relation types listed above.
* Only use entity IDs from the provided Entities list.
* Never invent entities.
* Never invent relation types.
* Never output relations with evidence such as "not mentioned", "implied", "no evidence", or "not provided".
* Only predict a relation if the text contains clear evidence for it.
* The evidence must be a short phrase copied from the text.
* Do not use world knowledge.
* Do not guess.
* If the relation is uncertain, do not output it.
* If no valid relations exist, return [].

Decision procedure:

For each possible pair of entities:

1. Check whether both entities appear in the provided Entities list.
2. Check whether the text contains evidence connecting them.
3. Check whether the evidence supports exactly one allowed relation type.
4. Output the relation only if all checks pass.

Return only valid JSON.
Do not include explanations.

Output format:

[
    {{
        "head": "E1",
        "tail": "E2",
        "type": "Work_For",
        "evidence": "short text snippet copied from the text"
    }}
]

Text:
{text}

Entities:
{entities}